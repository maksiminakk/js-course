"use strict";

const usdCurr = 28;
const euroCurr = 32;
const rubCurr = 0.5;

function convert (amount, curr) {
    console.log(amount * curr);
}


convert(500, usdCurr);
convert(12, euroCurr);
convert(78, rubCurr);

























